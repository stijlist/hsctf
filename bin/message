#!/usr/bin/env ruby
$LOAD_PATH.unshift("#{File.dirname(__FILE__)}/../lib")
require 'hsctf'

if !ARGV[0] || !ARGV[1]
  puts "Usage: message EMAIL MESSAGE"
  exit
end

email = ARGV[0]
message = ARGV[1]

m = Messager.new
m.send_message({email: email}, message)
